# ByPASS


## trace
The workload is constructed from a real RocksDB compaction run by collecting the raw host‑side I/O traces and computation cycles, then converting the cycles into device‑side processing time in our simulation framework.
